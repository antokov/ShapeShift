<?xml version="1.0" encoding="utf-8"?>
<!-- Dev backend (FastAPI) is plain HTTP on the local network / emulator host (10.0.2.2). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
